node_modules/
dist/
coverage/

# engines 运行时布局由 build.ts 生成(开发期是符号链接)
engines/bin/
engines/data/

# vendored pi-ai is consumed via its prebuilt dist — keep it in git
!packages/ai/dist/

.env
.env.*
!.env.example

# 工具的运行期产物:日志采集、gdb 会话转录、烧录记录
.my-pi/

*.log
*.tsbuildinfo
.DS_Store
.idea/
.vscode/
