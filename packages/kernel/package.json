{
  "name": "@yoma-desktop/kernel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The my-pi kernel seam: browser-safe view model + protocol + client, and the Node-side host that drives @yoma/my-pi",
  "exports": {
    ".": "./src/index.ts",
    "./host": "./src/host/index.ts",
    "./mypi": "./mypi.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test src"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:",
    "typescript": "catalog:"
  }
}
