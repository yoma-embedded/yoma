{
  "name": "@yoma-desktop/kernel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The Yoma kernel seam: browser-safe view model + protocol + client, and the Node-side host that drives @yoma/agent",
  "exports": {
    ".": "./src/index.ts",
    "./host": "./src/host/index.ts",
    "./kernel-alias": "./kernel-alias.ts",
    "./package.json": "./package.json"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "*"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@typescript/native-preview": "7.0.0-dev.20251207.1",
    "typescript": "5.8.2"
  }
}
