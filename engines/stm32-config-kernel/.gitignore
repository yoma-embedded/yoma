/target
/data.zip
# data/*.irpack(CubeMX 器件库经 stm32ck-import 解析出的 IR,27 族共 5.7 MB)**入库**:
# CI 出安装包的 runner 没有 CubeMX,不入库的话正式包里就没有任何 STM32 配置数据
# (v0.1.0~v0.1.4 都是这样发出去的)。重新生成:本机装 CubeMX(或设 STM32CK_CUBEMX_DB
# 指向其 db/ 目录)后删掉旧 pack 再跑 `bun engines/build.ts`,或直接
# `cargo run --release -p stm32ck-importer -- --all --out data`。
/data/*.debug.json
# HAL/CMSIS 固件组件(BSD-3,1.1GB)不进主仓 —— tools/fetch-fw.ps1 按需拉取
/data/fw/
*.snap.new
/proj*/
/cfg-*.json
