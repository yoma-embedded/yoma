All questions answered with evidence. Final report.

---

# ip_freertos findings (vs docs/middleware-gen-spec.md §2)

**Architecture headline (NEW_INSIGHT, affects all answers):** The Java plugin (`ip_freertos`) is GUI + object-table + footprint only. ALL code emission is data-driven: FreeMarker `.ftl` templates in `db/templates/` + per-family IP config XML in `db/mcu/config/FREERTOS-STM32F4xx_Configs.xml` + parameter DB `db/mcu/IP/FREERTOS-v8.0.0_Cube_Modes.xml` (all under `D:\Program Files\STMicroelectronics\STM32Cube\STM32CubeMX\`). The FTL engine and `RefConfigFile`/`Argument` runtime live in the absent host (`com.st.microxplorer.codegenerator`, `com.st.microxplorer.mcu`); the plugin's only codegen contract is pushing table contents into named Arguments:
```java
public void updateArguments(Middleware ip, String varName, String values) {
    for (RefConfigFile file : ip.getListOfRefConfigFiles()) {
        for (Argument arg : file.getArguments()) {
            if (!arg.getName().equalsIgnoreCase(varName)) continue;
            arg.setArgumentValue(values);
```
`D:/embedded_agent/java_code/decompiled/ip_freertos/com/st/microxplorer/plugins/ip/freertos/FreeRTOS.java` (L966–977)

## Q1. FreeRTOSConfig.h generation — TEMPLATE FILE, not string building

- Template: `db/templates/freertos_conf_v10_3_1.ftl`, bound by `<RefConfigFile Name="CONFIG" Template="freertos_conf_v10_3_1.ftl"><File Category="header" Name="FreeRTOSConfig.h" Version="10.3.1"/>` in `db/mcu/config/FREERTOS-STM32F4xx_Configs.xml` (L201–202). The macro list = that RefConfigFile's `<Argument>` list; values arrive as `SWIP.defines` name/value pairs (host resolves them from Modes.xml RefParameters + .ioc overrides).
- **configCPU_CLOCK_HZ — CONFIRMS_OURS but it's DB data, not computation:** Modes.xml: `<RefParameter Name="configCPU_CLOCK_HZ" ... DefaultValue="SystemCoreClock" Type="uniqueElementList">` (v8.0.0_Cube_Modes.xml L176; H7-D2 variant `DefaultValue="SystemD2Clock"` L169). Template: `#textern uint32_t ${valueCpuClock};` (L349) and `#define configCPU_CLOCK_HZ ( ${valueCpuClock} )` (L387).
- **configPRIO_BITS / LOWEST_PRIORITY — CONFIRMS_OURS, source = per-core DB rows, never derived:** `<RefParameter Name="configPRIO_BITS" DefaultValue="4" .../>` (L642; 2 for M0 L636, 3 for M33 L639); `<RefParameter Name="configLIBRARY_LOWEST_INTERRUPT_PRIORITY" DefaultValue="15" Max="15" .../>` (L610; M0 variant 3, L614); MAX_SYSCALL default 5 with `Max="=configLIBRARY_LOWEST_INTERRUPT_PRIORITY"` (L619). Template emits the fixed `#ifdef __NVIC_PRIO_BITS` block (ftl L539–561), whole Cortex-M section skipped for F0/G0/L0 (`[#if (familyName=="stm32g0") || ... ]` L536, "BZ 47214").
- **Conditional presence, not value substitution — CONTRADICTS_OURS (spec §2.1 table framing):** many macros are OMITTED entirely at default-0 rather than emitted with a value: `[#if configUSE_MUTEXES=="1"]#define configUSE_MUTEXES 1[/#if]` (L417), `[#if configCHECK_FOR_STACK_OVERFLOW !="0"]` (L421), configUSE_TIMERS block only when ==1 (L476–482). Always-emitted with value: PREEMPTION, IDLE_HOOK, TICK_HOOK, TICK_RATE, MAX_PRIORITIES, MINIMAL_STACK, TOTAL_HEAP (unless static-only), MAX_TASK_NAME_LEN, 16_BIT_TICKS, QUEUE_REGISTRY, PORT_OPTIMISED, CO_ROUTINES pair, 8 INCLUDE_* (L491–498). Our generator must reproduce omission when user sets e.g. STACK_OVERFLOW=0.
- **MEMORY_ALLOCATION mapping — NEW_INSIGHT:** `0→STATIC 0/DYNAMIC 1; 1→1/0 (and configTOTAL_HEAP_SIZE line omitted, ftl L400); 2→1/1` (ftl L373–384). DB default = 2 for F4 (`<RefParameter Name="MEMORY_ALLOCATION" DefaultValue="2" Type="list">` Modes L1072; G0/WB/G4 default 0, L1055) — explains reference's `STATIC=1/DYNAMIC=1` with no ioc key.
- **xPortSysTickHandler — CONFIRMS_OURS:** `[#if timeBaseSource?? && timeBaseSource!="SysTick"] #define xPortSysTickHandler SysTick_Handler [#else] /* #define ... */` (ftl L607–611; H7 uses timeBaseSource_M4/_M7, L581–605). NEW_INSIGHT: when timebase==SysTick a prototype `void xPortSysTickHandler(void);` is also emitted in the compiler-guard block (L336–353).
- `configTICK_RATE_HZ` cast is `portTickType` iff `CMSIS_version=="1.00"` else `TickType_t` (L388–392); DB CMSIS_version default for V1 = "1.02" (Modes L58) → TickType_t. CONFIRMS_OURS.

## Q2. Tables → freertos.c

- **ioc parsing (plugin):** keys list param `TasksKeys` (default literal `"Tasks01"`), value `;`-split then `,`-split via `StringUtils.splitPreserveAllTokens`; `<4` fields → `LOGGER.error("Incomplete task...")` skip; `<9` → `addIncompleteTask` (defaults `option="Default"`, `parameters="NULL"`, `Dynamic,NULL,NULL`); `==9` → complete (FreeRTOS.java L669–739). Priority stored in .ioc as V1 int (`osPriorityNormal→0`, Idle→−3 … Realtime→3, `Conversion.toIntegerV1`, Conversion.java L53–78) — CONFIRMS_OURS §2.2 mapping.
- **Codegen Argument format — NEW_INSIGHT (10 fields, not 9):** ThreadTable.addVariablesToIp inserts computed `generateStartFunction` at index 4: `tasks.append(name+","+priority); ...append(","+generateStartFunction); append(","+codegenOption);` and auto-prefixes parameters: `if (!validValue.startsWith("(void*)") && !"NULL".equals(validValue)) parameters = "(void*) " + validValue;` (ThreadTable.java L132–174). `generateStartFunction="0"` when the same entry function already appeared in an earlier row (`alreadyGenerated`, L185–195) → body/prototype emitted only once.
- **Codegen-mode literals — CONTRADICTS_OURS (spec says `Weak|External`):** actual strings are `"Default"`, `"As weak"`, `"As external"` (Properties.java L7–9), matched verbatim in templates (`[#if option == "As external"] extern void ${threadFunction}(...)` freertos_pfp.ftl L27–31; `[#if option == "As weak"] __weak void ...` freertos_body_user_threads_cmsis_v1.ftl L43–47). "As external" → extern prototype only, no body.
- **Emission structure (freertos_init_c.ftl):** creation order mutexes → recursive mutexes → `USER CODE RTOS_MUTEX` → binary semaphores → counting semaphores → `RTOS_SEMAPHORES` → timers → `RTOS_TIMERS` → queues → `RTOS_QUEUES` → threads → `RTOS_THREADS`. Dynamic: `osThreadDef(${threadName}, ${threadFunction}, ${threadPriority}, 0, ${threadStackSize}); ${threadName}Handle = osThreadCreate(osThread(${threadName}), ${threadArguments});` (L336–340). **Static extra code:** `osThreadStaticDef(name, fn, prio, 0, stack, ${threadBuffer}, &${threadControlBlock})` (L338) plus freertos_vars.ftl emits `uint32_t ${threadBuffer}[ ${threadStackSize} ]; osStaticThreadDef_t ${threadControlBlock};` (L48–52); queues: `uint8_t buf[size*sizeof(type)]; osStaticMessageQDef_t cb;` + `osMessageQStaticDef(...)`; semaphores/mutexes/timers get `osStatic*Def_t` + `os*StaticDef(...)`. Queue codegen adds a 4th `isInteger` field (QueueTable.java L121–128) that selects `sizeof(type)` vs literal size in buffer/create.
- **Default-task rules:** first row is the default task, tooltip "Can be modified but not removed" (ThreadTable.prepareRenderer L251); auto-added when state becomes CONFIGURED: `addItem("defaultTask", priority, configMINIMAL_STACK_SIZE, "StartDefaultTask", "Default", "NULL", "Dynamic", "NULL", "NULL")` (FreeRTOS.java L465). Deferred middleware init is emitted in the default (first) thread body, before its USER CODE section, for `USB_HOST | USB_DEVICE | LWIP(no MBEDTLS) | LoRaWAN | Sigfox | SubGHz_Phy`: `#t/* init code for ${mw} */ #tMX_${mw}_Init();` (freertos_body_default_thread_cmsis_v1.ftl L96–98) — CONFIRMS_OURS §2.2/§4.5.
- **Validation:** name uniqueness enforced across all 8 tables (`UserObjectsTab.checkNameIsValid` L184–204 → "multiple definition" popup); Dynamic task stack clamped: `if (stackSize < minimalStackSize) tableModel.setValueAt(minsize, i, 2)` and also reset if > param max (TasksAndQueuesTab.checkStackSize L525–553). `configMAX_PRIORITIES` min forced to `1 + max(taskPrio, USB_HOST.USBH_PROCESS_PRIO, LWIP.TCPIP_THREAD_PRIO)` (`adjustMaxPriorityForV1`, FreeRTOS.java L999–1065; `refParam.setMin((double)(1 + prioMin))`).
- **Footprint (gates mState="Error", not codegen):** TCB=96 (+8 task-notifications, +8 trace, +8 mutexes) (getTCBSize L1094–1107); task=TCB+stack*4; queue=76+16+n*elemSize (elemSize=4 if named type unresolvable, heapSizeForOneUserQueue L1229); mutex/sema=88 each (L1358–1362); timer=48; v2 static timer +8; event=32 (v2 only); + idle task & timer task/queue (76+12*len+16) when `MEMORY_ALLOCATION=="0"`; error iff `configTOTAL_HEAP_SIZE <= total` (isComputedSizeOK L262–290).

## Q3. Forced couplings — each rule's home

- **NVIC group→4: DB-driven, not Java — CONFIRMS_OURS:** `db/mcu/IP/NVIC-STM32F417_Modes.xml` L4–7: `<RefParameter Name="PriorityGroup" DefaultValue="NVIC_PRIORITYGROUP_4"><PossibleValue ... Value="NVIC_PRIORITYGROUP_4"/><Condition ... Expression="S_FREERTOS"/>` — when semaphore `S_FREERTOS` true the parameter has GROUP_4 as its only possible value; the 5-value variant is conditioned `!S_FREERTOS` (L8–14).
- **SVC/PendSV suppression:** vector table entries carry an RTOS flag: `<PossibleValue ... Value="SVCall_IRQn:Y,RTOS::NONE:"/>` and `"PendSV_IRQn:Y,RTOS::NONE:"` (same file L52–54); SysTick (`SysTick_IRQn:Y:::` L55) is NOT tagged — its RTOS takeover is computed by host `NvicVector.isManagedByRtos()`/`com.st.microxplorer.mcu.rtos.RtosManager` (**absent from tree**; plugin side only renders: `elem[2] = "Managed by RTOS";` and read-only cells, ip_nvic/NvicTable.java L225–227, L300–303). The `NVIC.Saved*IrqHandlerGenerated` ioc keys: nowhere in any plugin — host-side (absent), report as such.
- **Timebase warning — CONFIRMS_OURS (it's a warning, never forced):** `projectmanager/.../engine/ProjectBuilder.java` L1988–2008: `if (!CodeGenerator.isMCUDualCore() && rtos != null && rtos.isUsed() && sys != null && clMcu.getParser().checkCondition("S_SysTick")) timebaseOK = false;` message `"When RTOS is used, it is strongly recommended to use a HAL timebase source other than the Systick..."`, shown in the pre-generate warning dialog (L2404–2406). Trigger = semaphore `S_SysTick` (timebase source) AND FreeRTOS used; dual-core variants use `S_SysTick_M4/_M7`; ThreadX variant says "must".
- **TICK_INT_PRIORITY:** emitted by `db/templates/stm32f4xx_hal_conf_h.ftl` L143: `#define TICK_INT_PRIORITY [#if TICK_INT_PRIORITY??]${TICK_INT_PRIORITY}...U[#else]0x0FU[/#if]` — value is a host-computed template variable (computation absent from tree; reference behavior: equals the timebase IRQ preemption priority, 0 for TIM timebase, 15 for SysTick+RTOS).
- **Heap scheme — CONFIRMS_OURS:** `HEAP_NUMBER` RefParameter `DefaultValue="4"`, list 1/2/3/4/5 (+`heap_user` Value=10 w/ Semaphore `S_HEAP_USER` on `STM32Fxxx`) (FREERTOS-v8.0.0_Cube_Modes.xml L441–456). File copy: `<Component Name="Middleware:FREERTOS:HEAP:$HEAP_NUMBER"/>` in each RefConfig → `<SubComponent Csub="HEAP" Cvariant="4"><File ... Name="Source/portable/MemMang/heap_4.c" Condition="!S_DO_NOT_COPY_HEAP_FILE"/>` (Configs.xml L6, L108–110). No ioc key when default=4 — CONFIRMS spec note.

## Q4. CMSIS v1 vs v2 selection & differences

- **Selection:** plugin reads the Middleware Mode string: `if (mode.contains("V1")) newApiInUse="CMSIS_RTOS_V1" else if contains("V2") ... else if contains("NATIVE") ... else V1 default` (FreeRTOS.java setApiInUse L351–390); persisted/re-detected from .ioc VP pins: scans `Mcu.Pin<i>` for `VP_FREERTOS_VS_CMSIS_V2` / `VP_FREERTOS_VS_NATIVE` (H7 dual-core: `VP_<instance>_VS_...`) — absence of both ⇒ V1 (checkApiInUse L217–260). CONFIRMS_OURS (`VP_FREERTOS_VS_CMSIS_V1.Mode=CMSIS_V1`).
- **Wiring differences (Configs.xml L4–35):** v1 RefConfig `FREERTOS_ENABLE_Init` uses CONFIG/HOOKS/CREATION/VARS/PFP/DEFAULT_THREAD/USER_THREADS/RTOS_START; v2 `FREERTOS_CMSIS_V2_ENABLE_Init` swaps in `*_V2` templates, adds `TYPEDEFS_V2` and `RTOS_INIT` (`freertos_init_kernel.ftl`: emits `osKernelInitialize();` only `[#if CMSIS_version_integer gte 2]`) — included in main.c via `[@common.optinclude .../rtos_kernelInit.tmp/]` after USER CODE 2, before `MX_FREERTOS_Init();` then `rtos_kernelStart.tmp` (tpl_main_c.ftl L1300–1301, L1344–1351, L1377–1379). CONFIRMS_OURS §2.3 (no osKernelInitialize in v1).
- **v2 object code:** `${threadName}Handle = osThreadNew(${threadFunction}, ${threadArguments}, &${threadName}_attributes);` with `const osThreadAttr_t ..._attributes = { .priority = (osPriority_t) X, .stack_size = N * 4 }` (freertos_init_c_cmsis_v2.ftl L329, freertos_vars_cmsis_v2.ftl L59–68); osMutexNew/osSemaphoreNew(count,initial)/osTimerNew/osMessageQueueNew(size, sizeof(type))/osEventFlagsNew. Events exist only in v2 (`useEvents(){ return apiInUse.equals("CMSIS_RTOS_V2"); }` FreeRTOS.java L1500).
- **v2 parameter forcing (Modes.xml):** `configMAX_PRIORITIES DefaultValue="56"` uniqueElementList under `S_CMSIS_RTOS_V2$context` (L189–190, comment "configUSE_PORT_OPTIMISED_TASK_SELECTION is forced as disabled for CMSIS-RTOS v2"); `MEMORY_ALLOCATION` locked to "2" (L1038–1040); `configUSE_MUTEXES` two-value list only when `!S_CMSIS_RTOS_V2$context` (L280–281). v2 priority strings are absolute 1..56 (`Conversion.toIntegerV2`: Low=8, BelowNormal=16, Normal=24, AboveNormal=32, High=40, Realtime=48, +suffix 1–7).
- v1 kernel start: `[#if CMSIS_version == "1.00"] osKernelStart(NULL, NULL); [#else] osKernelStart(); [/#if]` (freertos_start.ftl L15–21) — with F4's CMSIS_version=1.02 → `osKernelStart();`. CONFIRMS_OURS.

## Q5. Middleware source-file contribution

`RefComponent Cclass="Middleware" Cgroup="FREERTOS" Cversion="10.3.1"` in `FREERTOS-STM32F4xx_Configs.xml` (L57–142), root `RootFolder="Middlewares/Third_Party/FreeRTOS/"` (L2):
- Kernel: all 18 `Source/include/*.h` enumerated + `croutine.c, event_groups.c, list.c, queue.c, stream_buffer.c (Condition="!configUSE_TASK_NOTIFICATIONS_DISABLED"), tasks.c, timers.c` (L80–86). **CONFIRMS_OURS §2.6 exactly.**
- API wrapper by semaphore: `Source/CMSIS_RTOS/cmsis_os.c|h Condition="S_CMSIS_RTOS_V1"`; `Source/CMSIS_RTOS_V2/cmsis_os2.c|h + cmsis_os.h + freertos_mpool.h + freertos_os2.h Condition="S_CMSIS_RTOS_V2"` (L89–97).
- Heap: `SubComponent Csub="HEAP" Cvariant="1..5"` selected by `$HEAP_NUMBER` (L99–113).
- **Port dir per toolchain, hard-coded per family — NEW_INSIGHT on mechanism:** `<SubComponent Csub="GCC"><File ... Name="Source/portable/GCC/ARM_CM4F/port.c" Condition="!S_FREERTOS_WITH_MPU"/>...ARM_CM4_MPU/... Condition="S_FREERTOS_WITH_MPU"/>` with IAR (adds `portasm.s`), Keil/RVDS → `RVDS/ARM_CM4F` (comment `<!-- F4 is based on Cortex-M4 -->`, L115–141). There is no Java core→port computation; each family's Configs.xml pins the CM dir. Our per-core rule should be a static family→`ARM_CM4F` table keyed by toolchain subcomponent (GCC for CMake/CubeIDE).
- `mpu_wrappers.c` only under `S_FREERTOS_WITH_MPU`; `heap_user.c` generated from `freertos_heap_user.ftl` under `S_HEAP_USER` (L144–146).

**Host-absent (call contract only):** FTL engine + `SWIPconfigModel` (`SWIPdatas`, `SWIP.defines`/`SWIP.variables`), `RefConfigFile/Argument` resolution, `NvicVector.isManagedByRtos()`, `RtosManager` (`com.st.microxplorer.mcu.rtos.RtosManager`, imported by projectmanager/ConfigFileManager.java L27), `Saved*IrqHandlerGenerated` persistence, and TICK_INT_PRIORITY value computation — all in `com.st.microxplorer.mcu`/`codegenerator`, not in this tree; behavior above inferred from the templates/DB they consume plus the reference project.