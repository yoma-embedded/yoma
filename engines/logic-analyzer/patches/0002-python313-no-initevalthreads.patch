--- a/libsigrokdecode4DSL/srd.c
+++ b/libsigrokdecode4DSL/srd.c
@@ -255,7 +255,9 @@
 	}
 
 	/* Initialize the Python GIL (this also happens to acquire it). */
+#if PY_VERSION_HEX < 0x030D0000
 	PyEval_InitThreads();
+#endif
 
 	/* Release the GIL (ignore return value, we don't need it here). */
 	PyEval_SaveThread();
